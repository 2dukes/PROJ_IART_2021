/*
    Position class which specifies the coordinates (x, y) of a position.
    x - x coordinate
    y - y coordinate
*/
class Position {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}